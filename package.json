{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:dev": "concurrently \"cd server && yarn run server\" \"cd client && yarn run dev\"",
    "start": "yarn run build && cd server && node server.js",
    "server": "cd server && nodemon server.js --ignore client",
    "build": "cd client && yarn run build",
    "install": "cd server && yarn install && cd ../client && yarn install",
    "client": "cd client && yarn start"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}

